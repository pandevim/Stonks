const Chart = () => {
  return (
    <div className="Chart" style={styles.chart}>
      chart
    </div>
  );
};

const styles = {
  chart: {
    margin: '10px',
    backgroundColor: '#ffffff'
  }
};

export default Chart;
